[project]
name = "languageai-backend"
version = "0.1.0"
description = "Dummy FastAPI backend for the LanguageAI talking avatar"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
