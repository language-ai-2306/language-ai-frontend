# Runtime
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
# Optional: enables real Claude replies. Unpinned to pull a recent SDK with
# structured outputs (messages.parse) and current model support.
anthropic>=0.49.0

# Dev / test
pytest==8.3.4
httpx==0.28.1
